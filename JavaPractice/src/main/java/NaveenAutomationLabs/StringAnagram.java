package NaveenAutomationLabs;

public class StringAnagram {
    /*
    anagram : art , rat.
    act, cat

    method-01:
    remove spaces replaceAll(" ", "")
    length check
    make the same case. lower case.
        get char array arr1
        get char array arr2

        Arrays.sort(arr1)
        Arrays.sort(arr2)
        check equals.
        Arrays.equals()
     */
}
