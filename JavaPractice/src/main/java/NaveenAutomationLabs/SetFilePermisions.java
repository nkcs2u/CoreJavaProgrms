package NaveenAutomationLabs;

public class SetFilePermisions {
    /*
    File file = new File(path);
    file.setExecutable(true);

    -rwxrwxrwx
    first: rwx of file owner
    second: rwx of group owner
    third: rwx of all other user
    - indicates file/directory

    ls -alt in linux to get file permission

    chmod 777 filname

    file.setReadable()
    file.setWritable()

    file.setExecutable(owneronly, true) ; for owner only // overloaded method
     */
}
