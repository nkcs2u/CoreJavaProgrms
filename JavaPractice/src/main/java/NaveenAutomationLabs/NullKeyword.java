package NaveenAutomationLabs;

public class NullKeyword {
    public static void main(String[] args){
        /*
        case sensitive null
        when only reference declaration is done, null ll come when print

        null u cannot assign to primitive value to another reference variable
        Integer a = null;
        int b = a; not allowd

       ClassObject obj = null;
       with obj can access to static method, but, not instance method
       because static method stored in common memory

       null == null , gives true
       null != null , false

        default value of String, null

        String s1 = (String)null;
        "null" result
         */
    }
}
