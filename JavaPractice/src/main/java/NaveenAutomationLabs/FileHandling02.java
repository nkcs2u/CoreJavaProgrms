package NaveenAutomationLabs;

public class FileHandling02 {

    /*

    // files and folders in sorted order

    String path = "dirrectory-path";

    File file = new File(path);
    //java.io

    File files[] = file.listFiles();
    Arrays.sort(files);

    for(File f: files){
        if(f.isFile()){
            //this is my file
        }if(f.isDirectory()){
            //folder
        }else{
            //not known
        }
    }


    // absolute path
    //canonical path
    //
     */
}
