package NaveenAutomationLabs;

public class CountDigits {
    /*
    HashMap<Integer , Integer> digimap = new HashMap<Integer , Integer> ();
    while(num > 0){
    // use modulator to get last digit
    // devide by 10 to remove last digit
    //proceed for all digits and start adding the counts

    // for single digit , String.valueOf() returns
    }
     */
}
