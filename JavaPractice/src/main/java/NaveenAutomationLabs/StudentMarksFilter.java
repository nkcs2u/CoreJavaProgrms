package NaveenAutomationLabs;

public class StudentMarksFilter {
    /*
    Create Student class:
        roll_num, name, marks
        constructor: initialize all variables.
        create getter and setter method
        @override toString() method : student details

        Another class:
            create Student objects in arraylist add s1, s2, s3
            List<Student> st = new ArrayList();

            total students - st.size()
            for(Studemt s : st){} //recurse through stdent objects

            st.stream().foreach()(e -> if(e.getmarks() > 80){ //print )

            st.stream().filter( e -> e.getMarks()>80).forEach(e -> print());

            st.stream().map(e -> e.getMarks()).max(Integer :: compare).get();ss
     */
}
