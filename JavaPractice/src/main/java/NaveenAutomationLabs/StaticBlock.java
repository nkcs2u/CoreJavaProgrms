package NaveenAutomationLabs;

public class StaticBlock {
    /*
    when static final variable printing, skips the class load - static block part.
    Instead it improves the performance by keeping static final variable is easy accessible.

    if no final, then static block runs
     */
}
