package NaveenAutomationLabs;

public class JDKVersions {
    /*
    1.8:
        . streams
        . on collection objects
        . Stream.of()   -> map/filter (process) -> collect

        . Optional class : to avoid null poinetr exception
        String[] arr = new String[5];
        arr[2].length() null ptr exception

        null handling.

    1.9:
        jshell commandline
        jshell --enable=preview

        FactoryMethod for immutable collections
        List.of(1, 2, 3)
        earlier Arrays.asList() method

    1,10:
        var keyword, to make common datatype
        var x = 10;
        x = "10";

    1.12:
        new switch case expression in jdk 12
        switch with lambda expression

    1.13:
        multiline string with 3 double colon

    1.14:
        record class
        constructor is not required, but, just record delaration is enough

    1.15:
        sealed class
        restrict the class by
        .
     */
}
