package NaveenAutomationLabs;

public class memory {
    /*
    heap : object creation memory

    stack : execution flow from main() method.
    next statments, local variables in method.

    if infinite loop/recursion, chances of stack overflow error.
     */
}
