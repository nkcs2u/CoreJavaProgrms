package NaveenAutomationLabs;

public class ReplaceSpacesString {
    /*

    trim() // remove spaces at start and end

    replaceAll("\\s+", "") // replaces all white spaces

    replaceAll("\\s", "") // performance is less

    StringUtils.deleteWhitespaces(input) //returns splaced string

    //without using inbuilt
    String str = ".. ..";
    for (int i=0; i<=str.length(); i++){
        if(str.charAt(i) != ' ' && str.charAt(i) != '\t'){
            op_str = op_str + str.charAt(i);
        }
    }

    method3
    take char arry replace white spaces by shifting

    stringbuffer & append

     */
}
