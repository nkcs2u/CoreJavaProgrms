package NaveenAutomationLabs;

public class FileHandler04 {
    /*

    //method 01
    File file = new File(path);
    if(Desktop.isDesktopSupported()){

    }

    Desktop desktop = Desktop.getDesktop();
    if(file.exists()){
        desktop.open(file);
    }


    //method 02
    File file = new File(path);
    FileInputstream fis = new FileInputstream(file);

    int c=0;
    while(c=fip.read()) != -1){
        // print (char)c
    }
    fip.close();


    //method-3
    File file = new File(path);
    BufferReader br = new BufferReader(new FileReader(file));
    int c=0;
    while((c = br.read()) != -1){
        (char)c;
    }

   br.close();


   //method 4
   FileReader fr = new FileReader(path);
   int c=0;
   while((c=fr.read())!= -1){
    (char)c;
    }

    //method 5
    File file = new File();
    Scanner sc = new Scanner(file);

    while(sc.hasnextLine()){
        sc.nextLine()
    }

    // method 6
    //nio
    List<String> li = Files.readAllLines(Paths.get(path) , StandardChartsets.UTF_8);
    iterate the list
     */
}
